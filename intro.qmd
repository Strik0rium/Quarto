# 导言

This is a book created from markdown and executable code.

![Compound Transformations](tikzplots/png/compound-transformations2.png){#fig-compound-transformations2 width="45%"}

中文输出测试。

```{python}
#| label: fig-scaled-versions
#| fig-cap: "Scaled versions of $[1, 2]^\\mathrm{T}$."
import numpy as np
import matplotlib.pyplot as plt

# Create 100 scaled versions of [1, 2]^T
scales = np.linspace(-4, 4, 100)
v1 = np.array([1, 2])
points = np.outer(scales, v1)  # shape (100, 2)

# Extract x and y coordinates
x = points[:, 0]
y = points[:, 1]

# Plot
fig, ax = plt.subplots(figsize=(8, 6))
ax.scatter(x, y, color='steelblue')
ax.axhline(0, color='gray', lw=1)
ax.axvline(0, color='gray', lw=1)
ax.set_xlabel("x", fontsize=16)
ax.set_ylabel("y", fontsize=16)
plt.show()
```

See @knuth84 for additional discussion of literate programming.
